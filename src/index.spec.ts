describe('Cardgame - Redux', () => {
    it('should shuffle the deck after shuffle action', () => {
        expect(true).toBe(true);
    });

    it('should shuffle the deck after shuffle action', () => {
        expect(true).toBe(true);
    });
})